= form_for @link do |f|
  -if @link.errors.any?
    #error_explanation
      %h2= "#{pluralize(@link.errors.count, "error")} prohibited this link from being saved:"
      %ul
        - @link.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :url
    = f.text_field :url

  -#.field
  -#  = f.label :title
  -#  = f.text_field :title
  .field
    = f.label :description
    = f.text_area :description

  -#.field
  -#  = f.label :url_thumbnail_path
  -#  = f.text_field :url_thumbnail_path
  -#.field
  -#  = f.label :tag_list
  -#  = f.text_field :tag_list
  -#.field
  -#  = f.label :published
  -#  = f.check_box :published
  .field
    = f.hidden_field :user_id, :value => current_user.id
  .actions
    = f.submit 'Save'
